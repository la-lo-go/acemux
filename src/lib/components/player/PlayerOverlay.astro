---
// Loading/error overlay for the video player
---

<div id="overlay" class="player-overlay">
  <div class="spinner"></div>
  <div id="statusText" class="status-text">Connecting to AceStream...</div>
  <div id="statusDetail" class="status-detail">Initializing stream</div>
  <div class="progress-bar">
    <div id="progressFill" class="progress-fill" style="width: 0%"></div>
  </div>
</div>

<style>
  .player-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.85);
    color: #fff;
    z-index: 10;
    padding: 20px;
    text-align: center;
    box-sizing: border-box;
  }
  
  .player-overlay.hidden { 
    display: none; 
  }
  
  .spinner {
    width: 40px;
    height: 40px;
    border: 3px solid rgba(255,255,255,0.1);
    border-left-color: #3b82f6;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 12px;
    flex-shrink: 0;
  }
  
  @media (min-width: 480px) {
    .spinner {
      width: 50px;
      height: 50px;
      border-width: 4px;
      margin-bottom: 16px;
    }
  }
  
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
  
  .status-text {
    font-size: 16px;
    margin-bottom: 8px;
    font-weight: 500;
  }
  
  .status-detail {
    font-size: 13px;
    opacity: 0.7;
    margin-bottom: 16px;
  }
  
  .progress-bar {
    width: 160px;
    max-width: 80%;
    height: 4px;
    background: rgba(255,255,255,0.1);
    border-radius: 2px;
    overflow: hidden;
    margin-top: 12px;
  }
  
  @media (min-width: 480px) {
    .progress-bar {
      width: 200px;
    }
  }
  
  .progress-fill {
    height: 100%;
    background: #3b82f6;
    border-radius: 2px;
    transition: width 0.3s ease;
  }
</style>
